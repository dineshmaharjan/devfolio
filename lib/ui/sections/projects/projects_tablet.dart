import 'package:flutter/material.dart';

class ProjectsTablet extends StatelessWidget {
  const ProjectsTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
       Image.asset("assets/images/kiosk_1.png"),
      ],
    );
  }
}
